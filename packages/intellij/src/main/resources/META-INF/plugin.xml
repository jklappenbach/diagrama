<idea-plugin>
    <id>dev.diagrama.intellij</id>
    <name>diagrama</name>
    <vendor>diagrama</vendor>
    <description><![CDATA[
        Live preview for <b>.diagrama.kdl</b> diagrams — system, gantt, class, state,
        sequence, and CI/CD — rendered next to the editor (Markdown editor+preview shape).
        Drag a shape in the preview to persist its position back to the file.
        Install <b>intellij-kdl</b> for KDL syntax highlighting in the editor pane.
    ]]></description>

    <depends>com.intellij.modules.platform</depends>

    <extensions defaultExtensionNs="com.intellij">
        <fileEditorProvider implementation="dev.diagrama.intellij.DiagramaEditorProvider"/>
    </extensions>
</idea-plugin>
